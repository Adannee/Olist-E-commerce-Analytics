version: 2

models:
  - name: fct_transactions
    columns:
      - name: transaction_id
        tests:
          - unique
          - not_null
      - name: account_id
        tests:
          - not_null
      - name: transaction_date
        tests:
          - not_null
  
  - name: dim_accounts
    columns:
      - name: account_id
        tests:
          - unique
          - not_null
  
  - name: dim_merchants
    columns:
      - name: merchant_id
        tests:
          - unique
          - not_null
          
          